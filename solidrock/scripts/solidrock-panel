#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="/usr/share/solidrock"
if [[ -d "$ROOT_DIR" ]]; then
  PANEL_MAIN="$ROOT_DIR/electron/panel/main.js"
else
  PANEL_MAIN="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)/electron/panel/main.js"
fi

electron "$PANEL_MAIN"
