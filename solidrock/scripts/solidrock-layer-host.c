/*
 * SolidRock Layer Host (placeholder)
 *
 * Este binário é responsável por criar uma surface zwlr-layer-shell para o painel.
 * A implementação completa deve integrar Wayland + layer-shell e repassar a UI
 * para o Electron.
 */

#include <stdio.h>

int main(void) {
  fprintf(stderr, "solidrock-layer-host: helper ainda não implementado.\n");
  return 1;
}
